{
  "scripts": {
    "dev": "python -m uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8010",
    "validate:css": "node scripts/validators/validate-css.js",
    "deploy": "bash scripts/deployment/deploy.sh"
  },
  "devDependencies": {
    "css": "^3.0.0",
    "postcss": "^8.5.10"
  },
  "dependencies": {
    "acorn": "^8.16.0",
    "playwright": "^1.60.0"
  }
}
